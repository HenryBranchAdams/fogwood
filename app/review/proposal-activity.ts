/** Pure review-copy projection for the page-owned proposal dock. */
// @ts-expect-error TS5097: Node's strip-types test loader resolves explicit source extensions.
export { proposalActivityDetail } from '../internal/surface-runtime.ts';
