# Workflow

Inspect the live canvas and any human geometry first. Stage this bounded branching map, stop at the page Apply or Reject gate, then inspect again after manipulation. When the story changes, preserve the prior branch and use a variant or annotation with explicit lineage. Never silently replace canon.
