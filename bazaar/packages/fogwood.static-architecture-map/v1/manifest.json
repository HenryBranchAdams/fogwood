{
  "id": "fogwood.static-architecture-map",
  "version": 1,
  "kind": "recipe",
  "content_hash": "sha256:100cc7a82692a734549cdc0e6ca68a4a39790f0751b779a961837dc116fbdf82",
  "title": "Architecture Map",
  "summary": "Explain the Fogwood people-agent-page-local store boundary without external effects.",
  "use_when": "Use when a team needs the local authority boundary and Apply or Reject flow visible on one canvas.",
  "not_for": "Not for executing generated work, writing the store directly, or granting an agent page authority.",
  "keywords": ["architecture", "people", "agent", "page", "store", "local", "apply", "reject", "boundary"],
  "input_schema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "audience": { "type": "string", "minLength": 1, "maxLength": 240 },
      "focus": { "type": "string", "maxLength": 240 }
    },
    "required": []
  },
  "output_schema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "artifact": { "type": "string", "enum": ["architecture-map"] },
      "review_required": { "type": "boolean" }
    },
    "required": ["artifact", "review_required"]
  },
  "ports": { "inputs": [], "outputs": [] },
  "permissions": ["read-local", "stage-proposal"],
  "locality": "local",
  "network": { "mode": "none", "endpoints": [], "telemetry": false },
  "limits": {
    "max_bytes": 49152,
    "max_instances": 8,
    "max_bindings": 16,
    "max_depth": 8,
    "max_operations": 8,
    "max_items": 48,
    "max_output_bytes": 49152
  },
  "recipes": ["recipes/static-architecture-map.json"],
  "skill": "SKILL.md",
  "prompts": ["prompts/architecture.md", "prompts/review.md"],
  "examples": ["examples/architecture-map.json", "examples/rejection.json"],
  "fixtures": [
    "fixtures/valid.json",
    "fixtures/malformed.json",
    "fixtures/oversized.json",
    "fixtures/loss.json"
  ],
  "sources": ["provenance/sources.json"],
  "license": "provenance/license.json",
  "notices": ["provenance/NOTICE.md"],
  "qualification": {
    "status": "fixture-tested",
    "evidence": ["provenance/qualification.json"],
    "reviewer": "Fogwood local catalog review",
    "date": "2026-08-26",
    "tested_boundary": "Data-only recipe compilation and bounded local proposal staging contract."
  },
  "compatibility": {
    "protocol": "fogwood-agent-runtime",
    "protocol_version": "1",
    "registry_version": "1",
    "renderer_ids": ["primitive.surface-block.v1", "primitive.native-shapes.v1"],
    "behavior_ids": [],
    "adapter_ids": []
  }
}
